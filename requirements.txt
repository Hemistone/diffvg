# diffvg runtime requirements (Py3.12 compatible)
#
# Notes
# - Torch GPU wheels depend on a CUDA-tagged index. See the CUDA section below.
# - Pins are set as compatible ranges to work across Linux/macOS with Python 3.12.
# - This file targets the library runtime. Example apps/dev extras are listed at
#   the bottom as commented lines; uncomment as needed.
# - For build-time helpers (wheel, setuptools, etc.), see
#   `requirements-build.txt` and the `[build-system]` section in `pyproject.toml`.

# Core runtime deps (from pyproject/poetry.lock, modernized for Py3.12)
numpy>=1.26
scikit-image>=0.23
pillow>=10
matplotlib>=3.8
svgpathtools>=1.6
cssutils>=2.9
cairosvg>=2.8

# Indices: use PyPI as default, add CUDA wheels as extra
--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cu130

# PyTorch (GPU) — pins must match your CUDA choice above
torch>=2.9.0
torchvision>=0.24.0


# ----------------------
# Optional: example apps
# ----------------------
# Uncomment as needed to run scripts in apps/.
piq # Support various perceptuals use in painterly_rendering.py
#tqdm>=4.66
#imageio>=2.31
#visdom>=0.2
#scipy>=1.11,<2.0
#pandas>=2.0
#seaborn>=0.13
#requests>=2.32
#pyzmq>=25
#pygame>=2.5
#ipython>=8.18
#wget>=3.2
scikit-learn>=1.7
#svgwrite>=1.4  # transitively from svgpathtools, listed here for convenience
